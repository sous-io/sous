node_modules/
dist/
coverage/
deprecated/
settings.local.js
/docs/notes

# Build state written by sous itself
proj.sous.state.json
sous.state.json
sous.pid

# Machine-specific env for the discovered .sous/ config. Note that the shared
# defaults layer, .sous/.env, is deliberately NOT ignored: it is committed.
.sous/.env.local

# Compiled agent instructions. These are OUTPUTS written by `xcv build`; the
# tracked sources live in .sous/prompts/. Root-anchored on purpose: the sources
# under shared-prompts/ and .sous/prompts/ are NOT affected by these rules.
/CLAUDE.md
/AGENTS.md
/.claude/
/.codex/
/docs/CLAUDE.md
