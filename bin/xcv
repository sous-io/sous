#!/usr/bin/env bash

# Dev convenience wrapper used by the repo's npm scripts (`./bin/xcv ...`).
# The published bin is run.js itself, which handles tsx registration.
exec node "$(cd -P "$(dirname "${BASH_SOURCE[0]}")" > /dev/null && pwd)/run.js" "$@"
